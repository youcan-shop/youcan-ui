export interface ColorInputProps {
  modelValue?: string
  preserveTransparency?: boolean
}

export interface ColorPickerProps {
  color: string
  defaults: string[]
  preserveTransparency?: boolean
}
